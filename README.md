# Task S108 Lambdas & Stream

## Level 1

### Exercise 1
I created a method that filters from a list of strings the strings that contains the letter o using lambda.

### Exercise 2
I created a method that filters from a list of strings the strings that contains the letter o and its minimum length is 6 using lambda.

### Exercise 3
I printed a list of months with a lambda.

### Exercise 4
I printed a list of months with method reference.

### Exercise 5
I created a functional interface with a method called `getPiValue` i instantiated the interface with a lambda and printed the value of pi.

### Exercise 6
I filtered and ordered with lambda and stream a list of numbers al words getting only the words and ordered from less length to mores length.

### Exercise 7
I do the same I did in the previous exercise but I ordered the words from more length to less length.

### Exercise 8
I reversed and String with a method in a functional interface that I implemented with a lambda.

## Level 2

#### Exercise 1

I filtered from a list of names the names that starts with `A` and haves three letters.

### Exercise 2
I made a method that return a list of numbers converted to a string separated in commas and adding a letter e into pair numbers and a o into impair numbers using lambda and stream.

### Exercise 3
I created a functional interface with a method to do a operation with two floats and later i implemented the operation in a lambda for add, subtract, multiply and divide.
